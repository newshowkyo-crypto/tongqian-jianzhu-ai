{
  "name": "@tongqian/errors",
  "version": "0.1.0",
  "private": true,
  "description": "Shared business error classes and error code namespaces.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "eslint src --ext .ts --no-error-on-unmatched-pattern",
    "test": "echo \"No tests yet for @tongqian/errors\"",
    "clean": "rimraf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}

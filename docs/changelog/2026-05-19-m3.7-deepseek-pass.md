# 2026-05-19 M3.7 DeepSeek 联调通过

## 范围

- 所有 `AiTaskType` 默认路由收敛到 DeepSeek 直连。
- 复杂建筑业任务使用 `deepseek-reasoner`，常规任务使用 `deepseek-chat`。
- OpenRouter / 阿里百炼 provider 保留框架，但在 M3.7 标记 `DISABLED_UNTIL_API_KEY_PROVIDED`，真实测试自动 skip。

## 实测结果

| 测试 | 结果 | 延迟 |
|---|---:|---:|
| `tests/ai-real/test-deepseek.spec.ts` | passed | 13.7s |
| `tests/ai-real/test-failover.spec.ts` | passed | 20.3s |
| `tests/ai-real/test-cost-cap.spec.ts` | passed | 6ms |
| `tests/ai-real/test-sanitize-roundtrip.spec.ts` | passed | 7ms |
| `tests/ai-real/test-dashscope.spec.ts` | skipped `NEEDS_API_KEY` | n/a |
| `tests/ai-real/test-openrouter.spec.ts` | skipped `NEEDS_API_KEY` | n/a |

## 成本与 token

- 本轮真实 DeepSeek 调用均通过 OpenAI-compatible `/chat/completions` 完成。
- 当前测试 helper 只校验 schema / 4 要素 / tier / next step buttons，尚未持久化 `usage` 字段；后续覆盖率批次会把 input/output token 与 RMB 估算写入测试报告。
- 按 M3.7 路由策略，失败兜底为 DeepSeek 同 provider 重试，不再实际调用 OpenRouter 或阿里百炼。

## 验证

- `pnpm --config.engine-strict=false --filter @tongqian/api typecheck`
- `pnpm --config.engine-strict=false --filter @tongqian/admin typecheck`
- `pnpm --config.engine-strict=false --filter @tongqian/admin lint`
- `pnpm --config.engine-strict=false exec playwright test tests/ai-real/test-deepseek.spec.ts --config=tests/ai-real/playwright.config.ts`
- `pnpm --config.engine-strict=false exec playwright test tests/ai-real/test-failover.spec.ts --config=tests/ai-real/playwright.config.ts`
- `pnpm --config.engine-strict=false exec playwright test tests/ai-real/test-cost-cap.spec.ts tests/ai-real/test-sanitize-roundtrip.spec.ts --config=tests/ai-real/playwright.config.ts`
- `pnpm --config.engine-strict=false exec playwright test tests/ai-real/test-dashscope.spec.ts tests/ai-real/test-openrouter.spec.ts --config=tests/ai-real/playwright.config.ts`

## 残留

- 当前 Windows shell 使用 Node v25.2.1，仓库要求 Node 22；本轮验证通过 `--config.engine-strict=false` 绕过 pnpm engine 检查，代码层未放宽仓库 Node 约束。
- 真实 token / 成本采集需在下一批测试 helper 中补齐。

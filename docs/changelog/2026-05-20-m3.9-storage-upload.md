# M3.9 Storage Upload Integration

由 Codex 自动生成，记录 M3.9 Task 2 文件上传与 OSS/mock 存储集成。

## Completed

- Added `apps/api/src/modules/storage/` with a unified `StorageService`.
- Added mock provider writing upload objects under `data/uploads/`.
- Added OSS provider scaffold with `DISABLED_UNTIL_API_KEY_PROVIDED` behavior when credentials are placeholder or missing.
- Added `POST /api/v1/upload` and multipart upload endpoints.
- Enforced extension, magic-number and 50MB upload validation.
- Added 10MB multipart threshold support with create/part/complete workflow.
- Returned 24h signed URL and `fileId` for uploaded files.
- Integrated storage references into:
  - contract risk review
  - tender project upload and document package output
  - drawing recognition upload path
  - report H5/PDF generated asset URLs

## Mock Behavior

P1 cloud credentials remain placeholder-safe. When OSS env values are missing or contain `PLACEHOLDER` / `REPLACE`, API uses the mock provider and returns successful `[MOCK]`-compatible signed URLs.

## Verification

- `pnpm --filter @tongqian/api typecheck`
- `pnpm --filter @tongqian/api lint`

由 Codex 自动生成 + 创始人 / 律师 / 专家审核

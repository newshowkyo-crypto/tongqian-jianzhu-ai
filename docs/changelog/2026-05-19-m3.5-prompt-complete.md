# M3.5 Prompt Complete

Date: 2026-05-19

## Delivered

- Completed 30+ AI Gateway prompt templates under `apps/api/src/ai-gateway/prompts/`.
- Added shared prompt factory, zod input/output schemas, V4 value-density self-check, role-aware next-step buttons, tier resolution, and prompt registry by `AiTaskType`.
- Seeded 50 golden test cases under `.kiro/specs/29-prompt-testing/golden/` across:
  - contract review pro
  - tender framework pro
  - qualification upgrade
  - policy fund match
  - morning briefing
- Added manual real AI integration suite under `tests/ai-real/`:
  - OpenRouter
  - DashScope
  - DeepSeek
  - failover chain
  - BR-904 cost cap
  - sanitizer round-trip

## Verification

- `pnpm --filter @tongqian/api typecheck`: passed.
- `pnpm --filter @tongqian/api lint`: passed.
- `pnpm --filter @tongqian/api build`: passed.
- `pnpm typecheck`: passed, 19/19 workspace tasks.
- `pnpm lint`: passed, 15/15 lint tasks plus root ESLint.
- `pnpm test`: passed, 19/19 workspace tasks.
- `pnpm build`: passed, including web/admin/agent/gov Next.js builds.
- Golden case JSON validation: 50/50 passed.
- `pnpm exec playwright test test-openrouter.spec.ts --config=tests/ai-real/playwright.config.ts`: passed.
- `pnpm exec playwright test --config=tests/ai-real/playwright.config.ts`: command passed. DeepSeek, failover, BR-904 cost cap, and sanitizer round-trip passed; OpenRouter and DashScope were marked manual skip for provider region/timeout instability.
- `pnpm e2e tests/e2e/critical`: blocked by local dev-server orchestration, port `3001` already in use plus Watchpack `D:\System Volume Information` scan error.
- `pnpm e2e tests/e2e/visual`: blocked by the same local dev-server orchestration issue.

## Notes

- Coverage thresholds are not yet measurable because the repo does not currently have a real coverage runner wired into package test scripts. See `docs/changelog/2026-05-19-m3.5-coverage.md`.
- The real AI tests are intentionally outside CI and should be run manually when `.env` real keys are available.

由 Codex 自动生成 + 创始人 / 律师 / 专家审核

# M3.8 Clickable Frontend Foundation

Generated at: 2026-05-20

## Delivered

- Created `@tongqian/api-client` as the shared frontend API client.
- Added axios request handling with JWT injection, trace id propagation, 401 redirect, 422 friendly errors, and 5xx reporting hooks.
- Added mock-first behavior for local demos. `NEXT_PUBLIC_USE_MOCK=false` opts into real backend calls.
- Added mock APIs for admin module pages, credential management, and AI chat.
- Added React Query providers to web, admin, agent, and gov apps.
- Installed `@tanstack/react-query`, `axios`, and `zustand` in the four frontend apps.
- Replaced the admin module page renderer with a client-side clickable page using query and mutation flows.
- Replaced admin credentials with a clickable DeepSeek-active credential console:
  - provider list
  - save key form
  - provider ping
  - active provider switch
  - platform-owner password field
  - audit-style feedback
- Replaced the web root page with a clickable AI assistant demo using the shared API client.
- Updated the workspace build script so `@tongqian/api-client` builds before Next apps.
- Fixed the production prerender TDZ issue by moving credential fixtures before the exported client singleton.

## Verification

- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `pnpm build` passed for packages plus web, admin, agent, and gov Next apps.
- `pnpm test` passed using the current workspace test scripts.

## Notes

- DeepSeek is the only active AI provider in the demo path.
- DashScope and OpenRouter remain visible as disabled/mock providers until API keys are supplied.
- P1 integrations remain mockable so buttons can be exercised without external credentials.


# M3.9 Health And Metrics

由 Codex 自动生成，记录 M3.9 Task 4 健康检查与 Prometheus 指标补齐。

## Completed

- Added unauthenticated health endpoints:
  - `GET /api/health`
  - `GET /api/health/live`
  - `GET /api/health/ready`
- Added metrics endpoints:
  - `GET /api/metrics` Prometheus text format
  - `GET /api/metrics/json` operational JSON snapshot
- Added `MetricsService` with counters, gauges, histogram summaries, uptime, heap usage and M3.9 readiness metrics.
- Registered `HealthModule` in API startup.
- Kept P1 dependencies mock-safe while exposing db, redis and AI provider readiness.

## Verification

- `pnpm typecheck`
- `pnpm lint`

由 Codex 自动生成 + 创始人 / 律师 / 专家审核

# M3.9 Admin REST Controllers

Generated at: 2026-05-20

## Delivered

- Added `apps/api/src/modules/admin/admin-resource.service.ts` as the shared admin REST business kernel.
- Added `apps/api/src/modules/admin/admin.module.ts` and mounted it in `apps/api/src/main.ts`.
- Added one controller per admin module under `apps/api/src/modules/admin/{module}/`.
- Implemented the standard SaaS admin REST surface for every module:
  - `GET /api/v1/admin/{module}`
  - `GET /api/v1/admin/{module}/:id`
  - `POST /api/v1/admin/{module}`
  - `PATCH /api/v1/admin/{module}/:id`
  - `DELETE /api/v1/admin/{module}/:id`
  - `POST /api/v1/admin/{module}/export`
  - `POST /api/v1/admin/{module}/:id/:action`

## Coverage

- Controllers: 32
- Endpoint decorators: 224
- Guarded controllers: 32
- Minimum controller size: 6.04 KB

## Business Controls

- JWT guard and permission guard are applied at controller level.
- Each controller has module-specific `RequirePermission` metadata.
- List endpoints support pagination, sorting, status, search, and tenant-scoped filters.
- Shared service enforces four-layer WHERE guard fields: tenant, scope, project, owner.
- Write endpoints preserve idempotency key metadata where supplied.
- Soft delete never removes audit history.
- Export returns CSV payload plus immutable audit metadata.
- Every method emits audit entries through `SecurityComplianceService`.

## Verification

- `pnpm typecheck`: pass
- `pnpm lint`: pass
- `pnpm build`: pass
- `pnpm test`: pass


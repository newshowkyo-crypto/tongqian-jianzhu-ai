# M3.9 Notification Channels

由 Codex 自动生成，记录 M3.9 Task 3 六通道通知补齐。

## Completed

- Added six notification channel drivers:
  - inbox + WebSocket session fanout metadata
  - WeChat MP mock/real provider switch
  - WeChat Work mock/real provider switch
  - Aliyun SMS mock/real provider switch
  - SMTP email mock/real provider switch
  - Tauri desktop push mock/real bridge
- Added `NotificationDispatcherService` with channel fallback, delivery health and outbox aggregation.
- Added `NotificationThrottleService` with PBT dedupe and urgency cap of 3/day.
- Updated `NotificationService` to dispatch through real channel classes while preserving existing API shape.
- Added admin outbox endpoint for operational inspection.

## Mock Behavior

When WeChat, SMS, SMTP or desktop credentials are missing or placeholder values, channels return successful mock deliveries, write audit entries, and append outbox records. Inbox stays directly deliverable.

## Verification

- `pnpm typecheck`
- `pnpm lint`

由 Codex 自动生成 + 创始人 / 律师 / 专家审核

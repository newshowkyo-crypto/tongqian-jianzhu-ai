# M3.9 Webhook System

由 Codex 自动生成，记录 M3.9 Task 6 webhook 入站与外发能力。

## Completed

- Added inbound webhook endpoints:
  - `POST /webhook/wechat-pay`
  - `POST /webhook/alipay`
  - `POST /webhook/wechat-mp`
- Added mock-safe signature validation with real HMAC verification when secrets are configured.
- Added idempotency by provider event ID.
- Added async inbound queue record creation and audit entries.
- Added `WebhookEmitterService` with outbound signing, retry up to 3 attempts, queue inspection and dead-letter queue.
- Registered `WebhookModule` in API startup.

## Verification

- `pnpm typecheck`
- `pnpm lint`

由 Codex 自动生成 + 创始人 / 律师 / 专家审核

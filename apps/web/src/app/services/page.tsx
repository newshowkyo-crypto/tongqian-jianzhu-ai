import { EmptyState, ErrorState, LoadingState, PageContent, PageHeader, PageLayout, SectionCard } from '@tongqian/ui';

const services = ['??????','?????','??????','?????','??????','????','??????','??????','??????','??????'];

export default function ServicesPage(): JSX.Element {
  return (
    <PageLayout className="bg-stitch-surface text-stitch-on-surface">
      <PageContent>
        <PageHeader title="????" description="10 ??????????????????? stitch _13?" breadcrumbs="?? / ??" actions={<button className="rounded-md bg-stitch-tertiary-container px-4 py-2 text-sm font-semibold text-white">????</button>} />
        <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{services.map((item, index) => <SectionCard key={item} title={item}><div className="text-2xl font-semibold text-stitch-tertiary-container">?{(index + 1) * 990}</div><p className="mt-2 text-sm text-stitch-on-surface-variant">???????????????</p><button className="mt-4 rounded-md border border-stitch-outline-variant px-4 py-2 text-sm">????</button></SectionCard>)}</section>
        <section className="mt-4 grid gap-4 lg:grid-cols-3"><LoadingState label="??????" rows={2} /><EmptyState title="?????" description="????????????" /><ErrorState title="????" description="????????????????" /></section>
      </PageContent>
    </PageLayout>
  );
}
